{
	"name": "angular-extended-builder-workspace",
	"version": "22.0.0",
	"private": true,
	"license": "MIT",
	"type": "module",
	"scripts": {
		"watch": "pnpm --parallel --filter=!app watch",
		"build": "pnpm --parallel --filter=!app build",
		"build-app": "ANGULAR_EXTENDED_BUILDER=debug pnpm --filter=app build",
		"serve": "pnpm --filter=app serve",
		"test": "pnpm --filter=angular-extended-builder test",
		"test:watch": "pnpm --filter=angular-extended-builder test:watch",
		"check": "oxlint . && oxfmt --check .",
		"format": "oxlint --fix . && oxfmt .",
		"release": "release-it"
	},
	"devDependencies": {
		"@commitlint/cli": "^20.5.0",
		"@commitlint/config-angular": "^20.5.0",
		"@release-it/bumper": "^7.0.5",
		"@types/node": "^24.12.2",
		"del-cli": "^7.0.0",
		"lefthook": "^2.1.5",
		"oxfmt": "^0.45.0",
		"oxlint": "^1.60.0",
		"oxlint-tsgolint": "^0.21.0",
		"release-it": "^19.2.4",
		"typescript": "6.0.3",
		"vitest": "^4.1.4"
	},
	"engines": {
		"node": ">=24"
	}
}
